{
  "name": "background-http-server",
  "description": "Write a server, run it in the background, hit it with curl, shut it down. Full background lifecycle.",
  "setup": "./scripts/setup.sh",
  "task": "Write a tiny Python HTTP server that responds with 'Hello from Yrden' on port 9876, run it in the background, curl it to prove it works, then shut everything down. Save the curl response to curl_result.txt.",
  "maxIterations": 20,
  "postconditions": {
    "completed": true
  },
  "extra_files": {}
}
