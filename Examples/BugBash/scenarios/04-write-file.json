{
  "name": "write-file",
  "description": "Create a new file following existing codebase patterns. File creation task.",
  "setup": "./scripts/setup.sh",
  "task": "Read src/justhtml/transforms_spec.py to understand the pattern used for transform dataclasses. Then create a new file src/justhtml/transforms_custom.py containing a frozen dataclass called AddClass that takes a CSS selector string and a class_name string, following the same @dataclass(frozen=True, slots=True) pattern used in transforms_spec.py.",
  "maxIterations": 22,
  "postconditions": {
    "completed": true,
    "file_exists": ["src/justhtml/transforms_custom.py"],
    "file_contains": {
      "src/justhtml/transforms_custom.py": ["AddClass", "dataclass", "selector", "class_name"]
    }
  },
  "extra_files": {},
  "denied_commands": []
}
