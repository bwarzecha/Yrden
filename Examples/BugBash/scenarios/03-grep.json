{
  "name": "grep",
  "description": "Search for usage of a type across the codebase. Pattern search task.",
  "setup": "./scripts/setup.sh",
  "task": "Use grep to find every place ParseError is referenced across the codebase. Write a report to parse-errors.md that lists each file where ParseError appears, how many times, and whether the file raises it, catches it, or just imports it.",
  "maxIterations": 22,
  "postconditions": {
    "completed": true,
    "file_exists": ["parse-errors.md"],
    "file_contains": {
      "parse-errors.md": ["ParseError", "tokenizer", "raise"]
    }
  },
  "extra_files": {},
  "denied_commands": []
}
