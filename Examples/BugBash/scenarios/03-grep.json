{
  "name": "grep",
  "description": "Tests the grep tool by asking the agent to search for a pattern across the codebase. At commit b9baf80, 'PathValidator' appears in multiple source files (BuiltInTools.swift, PathValidator.swift, ReadFileTool.swift, ShellTool.swift, WriteFileTool.swift) and test/doc files.",
  "setup": "./scripts/setup.sh",
  "task": "Find all files in the repository that contain the string 'PathValidator'. Show which files reference it and tell me how many files total contain it.",
  "maxIterations": 15,
  "postconditions": {
    "completed": true
  },
  "extra_files": {}
}
