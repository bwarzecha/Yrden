{
  "name": "grep",
  "description": "Search for usage of a type across the codebase. Pattern search task.",
  "setup": "./scripts/setup.sh",
  "task": "I want to understand how PathValidator is used across this codebase. Find every file that references it and give me a summary — which files define it, which ones instantiate it, and which ones just accept it as a dependency?",
  "maxIterations": 15,
  "postconditions": {
    "completed": true
  },
  "extra_files": {}
}
