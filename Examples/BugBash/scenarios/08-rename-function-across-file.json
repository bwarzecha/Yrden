{
  "name": "rename-function-across-file",
  "description": "Rename a function and all its call sites within a file. Multi-location rename task.",
  "setup": "./scripts/setup.sh",
  "task": "The function 'formatTokens' in Examples/AgentCLI/main.swift has a misleading name — it formats a count, not the tokens themselves. Rename it to 'formatTokenCount' everywhere it appears in that file.",
  "maxIterations": 15,
  "postconditions": {
    "completed": true,
    "file_contains": {
      "Examples/AgentCLI/main.swift": [
        "func formatTokenCount(",
        "formatTokenCount(u.inputTokens)",
        "formatTokenCount(u.outputTokens)",
        "formatTokenCount(u.totalTokens)"
      ]
    },
    "file_not_contains": {
      "Examples/AgentCLI/main.swift": [
        "formatTokens("
      ]
    }
  },
  "extra_files": {},
  "denied_commands": ["swift (build|test)", "xcodebuild"]
}
