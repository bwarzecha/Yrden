{
  "name": "read-file",
  "description": "Read a source file and explain its structure. Single-file comprehension task.",
  "setup": "./scripts/setup.sh",
  "task": "I need to call the shell tool from JSON but I'm not sure what parameters it accepts. Look at Sources/Yrden/Tools/ShellTool.swift and tell me every parameter the ShellToolArgs struct defines — I need to know the JSON key names I should use, not the Swift property names.",
  "maxIterations": 15,
  "postconditions": {
    "completed": true
  },
  "extra_files": {},
  "denied_commands": ["swift (build|test)", "xcodebuild"]
}
